# 第一条发布脚本（60秒 + B站延展版）

## 0. 统一开场/收尾（两个平台共用）

- 开场（0:00-0:03）：`一句钩子 + 结果钩`
- 收尾（0:56-1:00）：`关注 + 评论“教程” + 私信领取`

---

## 版本A：抖音 60s（9:16）

| 时间 | 画面 | 口播 | 屏幕文字 | 音效/字幕 |
| --- | --- | --- | --- | --- |
| 0:00-0:05 | 正脸出镜 + UI 对比图 | `同一张图，纯手工还原常常改半天还不准？` | `手工还原=熬夜` | 轻快提示音上扬 |
| 0:06-0:15 | 你在白板上写“问题清单”（耗时、易错、重复） | `尤其是 AI 生成图，传统抠图和重建，尺寸、间距和交互状态最容易乱。` | `传统方式：1-3小时` | 节拍停顿 |
| 0:16-0:35 | Screen Studio 录屏：Image2 从提示词到图稿 | `我先用Image2出一版UI，然后把图给技能。` | `Image2 → 还原图稿` | 机械键盘音 + click |
| 0:36-0:48 | HyperFrames 片段：导入并一键还原界面 | `看这个按钮，直接用 image2-ui-code-restorer 一键生成前端代码。` | `插件自动还原中` | 音乐过渡上扬 |
| 0:49-0:55 | 无插件版本对比（只保留头尾） | `我也试过纯手工，通常要反复拉伸+改位，最慢。` | `无插件` `插件` `效率差距` | 时间轴音效“滴答” |
| 0:56-1:00 | 正脸出镜 + 成果页 | `如果你要做 AI前端作品，关注我。评论“教程”，私信我把完整参数发你。` | `关注 + 评论教程` | 收尾上冲 |

## 版本B：B站 90秒解释版（16:9）

- 0:00-0:10：开场 + 目标（同抖音）
- 0:10-0:25：传统方案痛点与成本细化（列 3 个关键瓶颈）
- 0:25-0:45：Image2 生成过程 + 采样参数讲解
- 0:45-1:10：插件还原流程：输入图片 → 识别 → 生成 → 预览
- 1:10-1:25：对比（无插件手工流程起止）
- 1:25-1:35：失败场景演示（字体、间距、状态缺失）
- 1:35-1:50：结尾 CTA 与教程引导

## 可直接复用的口播字幕（逐句）

1. `今天给你看一条 60 秒路线：把 Image2 图片直接还原成前端页面。`
2. `传统方式很容易出事：要么改到半夜，要么做出来像模子一样。`
3. `我先用 image2 生成可识别的 UI 图，再让 restorer 直接吃图。`
4. `看，我把还原代码直接跑起来了，结构已经是可编辑状态。`
5. `同样的页面，对比一下无插件和有插件的时间成本。`
6. `你只要掌握输入规范和参数策略，效率可以明显上来。`
7. `关注我，评论“教程”，我把完整参数和素材模板给你。`

## 备注（剪辑）

- 左下角保留统一字幕色块，关键数字（例如“提升5-8倍”）用大字抛出
- 统一片尾固定 CTA 动画 2 秒
- 抖音建议每 12-18 秒放一个节奏点（吸引用户继续看）
